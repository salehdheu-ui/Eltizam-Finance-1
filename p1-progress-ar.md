# تقرير تطبيق الدفعة P1 لمنصة التزام

## الحالة الحالية

تم تطبيق دفعة P1 على فرع مستقل:

- **الفرع المحلي:** `manus/p1-ledger`
- **الفرع المنشور بعد الاعتماد:** سيتم رفعه إلى GitHub دون تعديل `main`
- **نطاق الدفعة:** ledger انتقالي، idempotency، تصحيح مالي، ميزانيات شهرية، تصدير بيانات، ومراقبة تشغيلية أساسية.

## ما تم تطبيقه

### سجل مالي دقيق وانتقالي

أُضيف جدول `ledger_entries` بقيمة `numeric(20,3)`، مصدر الحركة، العملة، المعرّف الخارجي، مفتاح منع التكرار، وقت حدوث الحركة، وربط الحركة العكسية. أُضيفت migration رقم 24 لإنشاء الجدول والفهارس وترحيل الحركات الموجودة، مع الإبقاء على جدول `transactions` متوافقاً مع الواجهة الحالية.

أصبحت الكتابات الجديدة من المسارات اليدوية والبنكية والدخل المتكرر والتحويلات والتسويات تكتب إدخالات ledger داخل نفس transaction مع تحديث الرصيد. إعادة محاولة حركة تحمل نفس `idempotencyKey` تعيد الحركة السابقة بدلاً من إنشاء نسخة جديدة.

### التصحيح المالي

أُضيفت أعمدة `reversal_of_id`, `reversal_reason`, و`voided_at` إلى المعاملات عبر migration رقم 25. أُضيف endpoint:

```text
POST /api/transactions/:id/reverse
```

ينشئ حركة عكسية ويرجع الرصيد، ويحافظ على الحركة الأصلية، ويربط ledger بالحركة العكسية، ويسجل الحدث في سجل التدقيق. كما تم منع حذف أصل أو حركة عكسية مرتبطة بسلسلة تصحيح مالي.

أضيف زر «عكس الحركة» في واجهة المعاملات مع إدخال سبب اختياري وشارة «معكوسة» للحركة الأصلية.

### الميزانيات الشهرية

أُضيف جدول `monthly_budgets` عبر migration رقم 26 مع قيد فريد لكل مستخدم وفئة وشهر. أُضيفت واجهات API:

```text
GET /api/budgets?month=YYYY-MM
PUT /api/budgets
```

أُضيفت صفحة `/budgets` لعرض الميزانية والمنفق والنسبة والفرق، مع إمكانية حفظ حد شهري لكل فئة. أُضيفت الصفحة إلى القائمة الرئيسية.

### تصدير البيانات والخصوصية

أُضيف endpoint:

```text
GET /api/data/export
```

ويقوم بتنزيل JSON يضم المحافظ، الفئات، المعاملات، الدخل المتكرر، الالتزامات، التعهدات، الأهداف، الميزانيات، أحداث البريد البنكي بعد تنقيح الحقول الحساسة، وقواعد التصنيف. لا يتضمن كلمات المرور أو رموز الجلسات أو أسرار التكامل. تم إضافة زر التنزيل في الإعدادات، مع تسجيل حدث `user.data_exported`.

### الاختبارات والمراقبة

أُضيفت اختبارات PostgreSQL اختيارية في `server/finance.integration.test.ts` تغطي idempotency، ledger للتحويل، rollback، وحذف الحركة. الاختبارات لا تستخدم أي قاعدة بيانات تلقائياً؛ تعمل فقط عند توفير `TEST_DATABASE_URL` حتى لا يتم لمس قاعدة المستخدم عن طريق الخطأ.

أُضيف `X-Request-Id` لكل طلب، ويظهر المعرف في سجلات API وفي استجابات الأخطاء ليسهل تتبع البلاغات. تم الحفاظ على سياسة عدم تسجيل محتوى مالي حساس.

## التحقق

| الأمر | النتيجة |
|---|---|
| `npm ci --no-audit --no-fund` | ناجح |
| `npm run check` | ناجح |
| `npm test` | ناجح — 10 اختبارات أساسية، مع 4 اختبارات تكامل متوقفة لعدم وجود `TEST_DATABASE_URL` |
| `npm run test:integration` | تم تشغيله بأمان — 4 اختبارات skipped لعدم وجود قاعدة اختبار |
| `npm audit --omit=dev --audit-level=high` | ناجح — 0 vulnerabilities |
| `npm run build` | ناجح |
| `git diff --check` | ناجح |

## ما يلزم قبل اعتبار P1 مكتملة بالكامل

ينبغي تشغيل الاختبارات الأربعة ضد قاعدة PostgreSQL اختبارية مستقلة، ثم تطبيق migration على نسخة staging ومقارنة مجموع ledger مع أرصدة المحافظ. كما أن استيراد البيانات لم يُفعّل بعد؛ المتاح في هذه الدفعة هو تصدير كامل آمن، بينما يحتاج الاستيراد إلى مرحلة معاينة وربط معرفات وتراجع كامل قبل السماح بالكتابة.

المرحلة التالية المقترحة هي إضافة مركز مراجعة موحد لفجوات الرصيد وحركات البريد غير المعتمدة، ثم تنفيذ استيراد JSON في وضع المعاينة، وبعده التحويل التدريجي للتقارير إلى ledger بدلاً من الاعتماد المباشر على `wallets.balance`.
